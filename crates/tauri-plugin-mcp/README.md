# `tauri-plugin-mcp`

![Crates.io Version](https://img.shields.io/crates/v/tauri-plugin-mcp)

MCP サーバーと連携するための Tauri プラグインです。

## インストール

このプラグインには Rust バージョン 1.77.2 以上が必要です。

`src-tauri/Cargo.toml`

```toml
[dependencies]
tauri-plugin-mcp = { version = "0.4.27" }
# または
tauri-plugin-mcp = { git = "https://github.com/moeru-ai/airi", tag = "0.4.27" }
```

JavaScript ゲストバインディングは、お好みの JavaScript パッケージマネージャーでインストールできます：

```bash
pnpm add @proj-airi/tauri-plugin-mcp
# または @antfu/ni を使用
ni @proj-airi/tauri-plugin-mcp
```

## 使い方

まず、プラグインを Tauri に登録する必要があります：

`src-tauri/src/lib.rs`

```rust
fn main() {
  tauri::Builder::default()
    .plugin(tauri_plugin_mcp::Builder.build())
    .run(tauri::generate_context!())
    .expect("error while running tauri application");
}
```

登録後、プラグインのすべての API が JavaScript ゲストバインディングを通じて利用可能になります：

```ts
import { callTool, connectServer, disconnectServer, listTools } from '@proj-airi/tauri-plugin-mcp'

// サンプル：AIRI Android を実行しているコンテナに接続
await connectServer('docker', 'run -i --rm -e ADB_HOST=host.docker.internal ghcr.io/lemonnekogh/airi-android:v0.1.0')

console.log(await listTools())
// [
//   {
//     description: 'Get the battery level of the device',
//     inputSchema: {
//       required: [],
//       title: 'battery_level',
//       type: 'object',
//     },
//     name: 'battery_level',
//   },
// ]

console.log(await callTool('battery_level', {}))
// {
//   content: [
//     {
//       type: 'text',
//       text: '100',
//     },
//   ],
//   isError: false,
// }

await disconnectServer()
```

## 開発

JavaScript バインディングのソースコードは [packages/tauri-plugin-mcp](../../packages/tauri-plugin-mcp/src/index.ts) にあります。

以下のファイルは `tauri-plugin` クレートから生成されています：

- `permissions/autogenerated`
- `permissions/schemas`

## TODO リスト

- [x] 単一リポジトリへの移行
- [ ] サーバー接続
  - [x] stdio
  - [ ] SSE
- [x] ツール
  - [x] ツール一覧
    - [x] シンプル：名前のみ
    - [ ] 説明、パラメータ、戻り値付き
  - [x] ツール呼び出し
    - [x] シンプル：名前のみ
    - [x] パラメータ付き
    - [x] 戻り値
    - [ ] 画像の戻り値
- [ ] プロンプト...
- [ ] リソース...
- [x] コマンドを便利に呼び出すための JavaScript パッケージ
