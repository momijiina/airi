# Satori Bot TODO リスト（最適化版）

## 🟡 P1: アーキテクチャ改善とロジック最適化（パフォーマンスと体験）

### 1. Action 切り捨てロジックのリファクタリング、コンテキストの記憶喪失防止
**目標**: LLM の履歴切り捨てがロジックチェーン（例：`send_message` + `continue`）を破壊しないようにする。
- [ ] **戦略層（上級）**:
    - [ ] 古い `actions` を直接破棄せず、テキスト Summary に圧縮して LLM Context に保存する試み。

## 🟢 P2: 機能拡張と型安全性

- [ ] **監視強化**:
    - [ ] すべての Action 実行に対してより詳細な Trace ログを追加。
- [ ] **型の厳格化**:
    - [ ] 残留する `as any` 型アサーションを継続的にチェックし排除。

---

## ✅ 完了事項（アーカイブ）

### 🛡️ 1. コア安定性と並行アーキテクチャ（P0）
- [x] **短期記憶クリーンアップの競合状態を修正**: ID ベースの精密削除を実装し、非同期メッセージの消失を防止。
- [x] **グローバルロックのデッドロックを排除**: ロック粒度を Channel レベルに下げ、`try...finally` による強制解放メカニズムを導入。
- [x] **ノンブロッキングスケジューリングを実装**: `onMessageArrival` と周期タスクを並行実行に変更し、スループットを向上。

### ⚙️ 2. ロジックチェーンの完全性と体験最適化（P1）
- [x] **スマート Action 切り捨て**: `trimActions` の自動巻き戻しを実装し、`continue` などのペアロジックチェーンの切断を防止。
- [x] **API 乱用の防止**: `handleLoopStep` に `MAX_LOOP_ITERATIONS = 5` のハードリミットを追加。

### 💎 3. 型安全性とインフラストラクチャ（P2）
- [x] **Satori API の型不一致を修正**: `SatoriMessageCreateResponse` のインターフェース定義をランタイム Schema と一致させた。
- [x] **グローバル強制終了を削除**: `process.on('unhandledRejection')` から `process.exit(1)` を削除。
- [x] **Any 型の乱用を排除**: LLM パース、データベース Schema などの複数箇所の型劣化を修正。
- [x] **キュー永続化 I/O を書き換え**: Drizzle ORM のインクリメンタル更新パターンを実装。
